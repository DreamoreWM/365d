<h4>{{ prestation.typePrestation ?? prestation.bonDeCommande.typePrestation }}</h4>

<p><strong>Date :</strong> {{ prestation.datePrestation|date('d/m/Y') }}</p>

<p><strong>Description :</strong><br>
    {{ prestation.description }}
</p>

<p>
    <strong>Statut : </strong>
    <span class="badge 
        {% if prestation.statut == 'terminé' %} bg-success
        {% elseif prestation.statut == 'en cours' %} bg-primary
        {% elseif prestation.statut == 'programmé' %} bg-info
        {% elseif prestation.statut == 'non effectué' %} bg-danger
        {% else %} bg-warning {% endif %}">
        {{ prestation.statut }}
    </span>
</p>

<hr>

<h5>Prévisualisation PDF</h5>

<iframe 
    src="{{ path('prestation_pdf', { id: prestation.id }) }}"
    style="width:100%; height:80vh; border:0;">
</iframe>

<div class="text-end mt-3">
    <a href="{{ path('prestation_pdf', { id: prestation.id }) }}" 
       target="_blank"
       class="btn btn-secondary">
       📄 Télécharger le PDF
    </a>
</div>
