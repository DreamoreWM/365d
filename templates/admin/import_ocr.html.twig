{% extends '@EasyAdmin/page/content.html.twig' %}

{% block content %}
    <div class="container" style="max-width: 600px;">
        <h2 class="mb-4 text-center">📸 Importer un bon de commande via OCR</h2>

        <form method="post" enctype="multipart/form-data" class="shadow p-4 rounded bg-white">
            <div class="mb-3">
                <label for="photo" class="form-label fw-bold">Sélectionnez une image :</label>
                <input type="file" name="photo" id="photo" required class="form-control">
            </div>

            <div class="d-flex justify-content-between mt-4">
                <button class="btn btn-primary">Analyser et préremplir</button>
                <a href="{{ backUrl }}" class="btn btn-secondary">Retour</a>
            </div>
        </form>
    </div>
{% endblock %}
